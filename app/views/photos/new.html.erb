<%#= form_for @photo, :url => photos_path do |f| %>
<%= form_for @photo, :url => photos_path, :html => { :multipart => true, :role => "form" } do |f| %>
    <%= render :partial => 'form', :locals => { :f => f } %>

    <%= f.submit "Create" %>


<% end %>